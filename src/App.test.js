/* import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import App from './App';
import WeatherExtraInfo from './components/WeatherExtraInfo'; */


test("go away until I need you", () => {
  expect(1).toEqual(1);
});
